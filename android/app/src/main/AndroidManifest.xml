<manifest xmlns:android="http://schemas.android.com/apk/res/xml"
    package="com.innomind.bestverifier">

    <queries>
        <package android:name="in.net.uidai.qrcodescanner" />
        <package android:name="in.gov.uidai.pehchaan" />
        <package android:name="in.gov.uidai.facerd" />
    </queries>

    <application
        android:label="Best Verifier"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false">
        
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>
</manifest>
