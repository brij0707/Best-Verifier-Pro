name: best_verifier
description: "A comprehensive private directory for identity and asset verification."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Updated to the latest stable version suggested by your build logs
  external_app_launcher: ^4.0.3

  # Other verified dependencies
  url_launcher: ^6.2.5
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
